detect-secrets
import-linter
# CommonMark parsing of issue bodies / ADR records in scripts/validate_issue_sections.py.
# Was a transitive of rich; declared explicitly because we call its API directly (#426).
# Keep this file ASCII: pip-compile reads it in the OS codepage and dies on Cyrillic.
markdown-it-py
mypy
pip-audit
pip-tools
pytest
pytest-cov
# The role catalogue is read directly by scripts/agent_orchestrator.py and by the
# hand-off gate in scripts/validate_issue_sections.py, so the runtime dependency is
# declared rather than left transitive; types-PyYAML alone typed an import nothing
# guaranteed to be installed (#474, #406).
PyYAML
ragas
ruff
types-PyYAML
types-requests
