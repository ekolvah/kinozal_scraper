"""Build helpers for the Claude Code plugin export payload."""

from __future__ import annotations


def transform_payload(source: str) -> str:
    """Return source unchanged until the plugin export transforms are implemented."""
    return source
